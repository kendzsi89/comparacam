<div class="footer">
    <div class="counter-section">
        <% Object.values(photos).forEach(photo => { %>
        <span id="counter-<%=photo.brand%>">
          <img class="logo" src="/assets/images/<%= photo.brand%>.png"><p class="likes-of-<%= photo.brand%>"></p>
        </span>

       <% }); %>
        
    </div>
    <a href="https://www.unsplash.com">Photos provided by Unsplash</a>
</div>
<script>
  let cameras = [
    "canon",
    "nikon",
    "fujifilm",
    "sony",
    "other",
  ]

  
  $(document).ready(function() {
  cameras.forEach((camera)=>{
    function toggleHandle(){
      $("#counter-" + camera).toggleClass("opacity-half")
      $("#column-" + camera).toggleClass("hidden")
    
    }

    if (localStorage.getItem('switch-' + camera) && localStorage.getItem('switch-' + camera) === "true") {
    toggleHandle(); 
    }
    $("#counter-" + camera).click(function() {
      toggleHandle();
      localStorage.setItem("switch-" + camera, $("#counter-" + camera).hasClass("opacity-half"));
    })
    

      $("#like-button-" + camera).click(function() {
      counter = localStorage.getItem(camera)
      counter++
      localStorage.setItem(camera, counter)
    
    })
    $(".likes-of-" + camera).text(localStorage.getItem(camera))
  })
  })



</script>
</body>
